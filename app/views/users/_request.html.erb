<%= form_with(model: current_user.inverse_friendships.build, local: true) do |f| %>
  <div>
  <%= hidden_field_tag :friend_id, @user.id %>
    <%= hidden_field_tag :confirmed, false %>
  </div>
  <%= f.submit "Invite to friendship", class: "btn btn-primary" %>
<% end %>